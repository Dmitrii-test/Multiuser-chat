package ru.dmitrii.utils.models;

public enum MessageType {
    CONNECT,
    NAME_REQUEST,
    USER_LOGIN,
    USER_SIGNUP,
    NAME_ACCEPTED,
    TEXT,
    USER_ADDED,
    USER_REMOVED,
    SERVER_DISCONNECT;
}
